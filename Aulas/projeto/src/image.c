#include <stdio.h>
#include <stdlib.h>
#include "image.h"

// Função para ler imagem PGM
Image* readPGM(const char* filename) {
    // Implementação de leitura de imagem PGM
}

// Função para salvar imagem PGM
void savePGM(const Image* img, const char* filename) {
    // Implementação de salvamento de imagem
}

// Função para liberar a memória da imagem
void freeImage(Image* img) {
    // Implementação para liberar memória alocada dinamicamente
}
