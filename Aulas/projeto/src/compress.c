#include <stdio.h>
#include "compress.h"
#include "utils.h"

// Função de compressão que divide a imagem em blocos
void compressBlock(int** pixels, int x, int y, int blockSize, double threshold, FILE* bitstream) {
    // Implementação da compressão
}

// Função para reconstruir a imagem a partir do bitstream
void reconstructImage(const char* bitstreamFilename, const char* outputPGM, int width, int height) {
    // Implementação da reconstrução
}
